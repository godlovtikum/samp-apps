// No public Supabase keys are needed anymore — the browser only talks to
// Netlify Functions. This file is intentionally empty and kept as a stable
// reference for any future client-side feature flags.
window.SAMP_CONFIG = {
  pollIntervalMs: 12_000, // 10–20s as per spec
};
